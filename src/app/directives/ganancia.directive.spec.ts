import { GananciaDirective } from './ganancia.directive';

describe('GananciaDirective', () => {
  it('should create an instance', () => {
    const directive = new GananciaDirective();
    expect(directive).toBeTruthy();
  });
});
